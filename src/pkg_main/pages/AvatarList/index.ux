<template>
  <div class="page-container">
    <!-- Header -->
    <div class="header">
      <div class="back-btn" onclick="goBack">
        <image class="back-icon" src="/pkg_main/assets/img/back.png"></image>
      </div>
      <text class="title">{{pageTitle}}</text>
      <div class="placeholder"></div>
    </div>

    <!-- Avatar Grid -->
    <list class="list-content">
      <list-item type="grid" class="grid-row" for="{{avatarRows}}">
        <div class="grid-cell" onclick="openAvatar($item.left)">
           <image class="avatar-img" src="{{$item.left}}"></image>
        </div>
        <div class="grid-cell" if="{{$item.right}}" onclick="openAvatar($item.right)">
           <image class="avatar-img" src="{{$item.right}}"></image>
        </div>
        <div class="grid-cell-empty" if="{{!$item.right}}"></div>
      </list-item>
    </list>
  </div>
</template>

<script>
import router from '@system.router'

export default {
  data: {
    pageTitle: '头像列表',
    type: 'latest',
    avatarRows: []
  },
  onInit() {
    if (this.pageTitle) {
      this.pageTitle = this.pageTitle;
    }
    if (this.type) {
      this.type = this.type;
    }
    this.loadData();
  },
  loadData() {
    const baseUrl = 'https://cdn.shijinzhuang.com/fytk/tx/';
    let folder = this.type === 'popular' ? 'rm' : 'zx';
    
    const rows = [];
    for (let i = 1; i <= 20; i += 2) {
      rows.push({
        left: `${baseUrl}${folder}/${i}.jpg`,
        right: `${baseUrl}${folder}/${i + 1}.jpg`
      });
    }
    this.avatarRows = rows;
  },
  goBack() {
    router.back();
  },
  openAvatar(url) {
    router.push({
      uri: '/pkg_main/pages/AvatarDetail',
      params: {
        imgUrl: url
      }
    })
  }
}
</script>

<style>
.page-container {
  flex-direction: column;
  width: 100%;
  height: 100%;
  background-color: #121212;
}

.header {
  height: 100px;
  padding-top: 30px;
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
  padding-left: 16px;
  padding-right: 16px;
  background: linear-gradient(to bottom, #66cc33, #121212); 
}

.back-btn {
  width: 30px;
  height: 30px;
  justify-content: center;
  align-items: center;
  background-color: #ffffff;
  border-radius: 15px;
}

.back-icon {
  width: 16px;
  height: 16px;
  object-fit: contain;
}

.title {
  font-size: 18px;
  color: #ffffff;
  font-weight: bold;
}

.placeholder {
  width: 30px;
}

.list-content {
  flex: 1;
  padding: 10px;
}

.grid-row {
  flex-direction: row;
  justify-content: space-between;
  margin-bottom: 10px;
}

.grid-cell {
  width: 48%;
  height: 105px;
  border-radius: 10px;
  background-color: #333333;
  overflow: hidden;
}

.grid-cell-empty {
  width: 48%;
}

.avatar-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 10px;
}
</style>
